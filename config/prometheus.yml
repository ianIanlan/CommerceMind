# Prometheus 配置文件 — CommerceMind 智能客服系统

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'commercemind'
    environment: 'production'

rule_files:
  - "alerts/*.yml"

scrape_configs:
  # CommerceMind 主应用指标
  - job_name: 'commercemind-app'
    static_configs:
      - targets: ['commercemind:8000']
        labels:
          service: 'commercemind'
    scrape_interval: 10s
    metrics_path: '/metrics'

  # Prometheus 自监控
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
